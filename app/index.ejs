<!doctype html>
<html lang="en">
<head>
  <base href="/aurelius">
  <!-- The first thing in any HTML file should be the charset -->
  <meta charset="utf-8">
  <!-- Make the page mobile compatible -->
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Allow installing the app to the homescreen -->
  <link rel="manifest" href="manifest.json">
  <meta name="mobile-web-app-capable" content="yes">
  <title>Aurelius</title>
  <% if (htmlWebpackPlugin.options.config) { %>
    <script type="text/javascript">
      window.config = <%= JSON.stringify(htmlWebpackPlugin.options.config) %>;
    </script>
  <% } %>
</head>
<body>
<!-- The app hooks into this div -->
<div id="app"></div>
<!-- A lot of magic happens in this file. HtmlWebpackPlugin automatically includes all assets (e.g. bundle.js, main.css) with the correct HTML tags, which is why they are missing in this HTML file. Don't add any assets here! (Check out webpackconfig.js if you want to know more) -->
<% if (htmlWebpackPlugin.options.dllNames) { %>
  <% for(var i=0; i < htmlWebpackPlugin.options.dllNames.length; i++) { %>
    <script data-dll='true' src='/<%= htmlWebpackPlugin.options.dllNames[i] %>.dll.js'></script>
  <% } %>
<% } %>
</body>
</html>
